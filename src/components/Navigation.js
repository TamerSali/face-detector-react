import React from 'react'
import { Button } from '@material-ui/core'

export default function Navigation() {
    return (
        <nav className="sign">
            <Button size="large" variant="outlined">Sign Out</Button>
        </nav>
    )
}
